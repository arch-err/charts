apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hookshot.fullname" . }}-registration
  labels:
    {{- include "hookshot.labels" . | nindent 4 }}
data:
  registration.yml: |
    {{- .Values.registration | toYaml | nindent 4 }}
