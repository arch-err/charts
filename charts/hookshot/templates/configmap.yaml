apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hookshot.fullname" . }}-config
  labels:
    {{- include "hookshot.labels" . | nindent 4 }}
data:
  config.yml: |
    {{- .Values.config | toYaml | nindent 4 }}
